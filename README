Meddela - CAN message and signal definition in JSON
===================================================

Define CAN messages and their signals in JSON and then use Meddela to generate
code for CAN node configuration, routing and serialization etc.

Meddela doesn't provide any microcontroller code or configuration, that has to
be done by the user but the provided examples may be a good start.